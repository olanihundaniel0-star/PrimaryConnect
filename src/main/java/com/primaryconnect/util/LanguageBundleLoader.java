package com.primaryconnect.util;

/**
 * Loads the language string bundle from the application's resource files into the language engine.
 */
public class LanguageBundleLoader {
    public LanguageBundleLoader() {
    }
}
